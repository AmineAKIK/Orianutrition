import { useParams } from 'react-router-dom'; import { StandardPage } from './StandardPage'; import { articles } from '../data/articles'; import { NotFoundPage } from './NotFoundPage'
export function ArticlePage(){const{slug}=useParams();const article=articles.find(item=>item.slug===slug);if(!article)return <NotFoundPage/>;return <StandardPage eyebrow={article.category} title={article.title} intro={article.excerpt}><article className="max-w-3xl">{article.body.map(paragraph=><p key={paragraph} className="mb-5 text-muted leading-relaxed">{paragraph}</p>)}{article.disclaimer&&<p className="mt-8 border-t border-sage pt-5 text-sm text-muted">{article.disclaimer}</p>}</article></StandardPage>}
