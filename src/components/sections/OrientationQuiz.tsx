import { useState } from 'react'
import { X } from 'lucide-react'
import { Button, LinkButton } from '../ui/Button'
const questions=[{title:'Ton planning change-t-il souvent ?',options:['Rarement','D’une semaine à l’autre','Presque chaque jour']},{title:'Quel est ton sujet prioritaire ?',options:['Organisation des repas','Énergie et récupération','Un point très ciblé']},{title:'Quel niveau de suivi recherches-tu ?',options:['Faire le point','Être accompagné plusieurs semaines','Résoudre une question précise']}] as const
export function OrientationQuiz({isOpen,onClose}:{isOpen:boolean;onClose:()=>void}){const[step,setStep]=useState(0);if(!isOpen)return null;const done=step>=questions.length;return <div className="fixed inset-0 z-[80] grid place-items-center bg-forest-dark/60 p-4" role="dialog" aria-modal="true" aria-labelledby="orientation-title"><div className="w-full max-w-xl bg-paper p-6 sm:p-8"><div className="flex items-start justify-between gap-4"><div><p className="eyebrow">Orientation</p><h2 id="orientation-title" className="mt-3 text-3xl">Trouver le bon point de départ</h2></div><button type="button" onClick={onClose} aria-label="Fermer" className="grid min-h-11 min-w-11 place-items-center"><X aria-hidden="true"/></button></div>{done?<div className="mt-8"><p className="text-muted leading-relaxed">Un premier échange permet de choisir le format adapté à ton niveau de besoin et à ton planning.</p><div className="mt-6"><LinkButton to="/contact">Prendre contact</LinkButton></div></div>:<div className="mt-8"><p className="font-serif text-xl">{questions[step].title}</p><div className="mt-5 grid gap-3">{questions[step].options.map(option=><Button key={option} type="button" variant="secondary" onClick={()=>setStep(value=>value+1)} className="justify-start">{option}</Button>)}</div></div>}</div></div>}
