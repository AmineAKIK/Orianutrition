import { Container } from '../ui/Container'; import { SectionHeading } from '../ui/SectionHeading'; import { LinkButton } from '../ui/Button'; import { ArticleCard } from '../cards/ArticleCard'; import { articles } from '../../data/articles'
export function BlogPreview(){return <section className="section-pad bg-sage-light"><Container><SectionHeading eyebrow="Conseils" title="Comprendre ce qui se joue quand le rythme se décale."/><div className="mt-10 grid md:grid-cols-3 gap-5">{articles.slice(0,3).map(article=><ArticleCard key={article.slug} article={article}/>)}</div><div className="mt-8"><LinkButton to="/conseils">Lire les conseils</LinkButton></div></Container></section>}
