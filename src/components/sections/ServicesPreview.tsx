import { Container } from '../ui/Container'; import { SectionHeading } from '../ui/SectionHeading'; import { LinkButton } from '../ui/Button'; import { ServiceCard } from '../cards/ServiceCard'; import { servicePacks } from '../../data/services'
export function ServicesPreview(){return <section className="section-pad bg-sage-light"><Container><SectionHeading eyebrow="Accompagnements" title="Choisir un format adapté à ton besoin du moment."/><div className="mt-10 grid md:grid-cols-3 gap-5">{servicePacks.map(service=><ServiceCard key={service.slug} service={service}/>)}</div><div className="mt-8"><LinkButton to="/accompagnements">Voir les accompagnements</LinkButton></div></Container></section>}
