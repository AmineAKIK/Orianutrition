import type { ServicePack } from '../../data/services'
export function ServiceCard({service}:{service:ServicePack}){return <article className={`border p-7 ${service.featured?'border-forest bg-sage-light':'border-sage bg-paper'}`}><div className="flex items-start justify-between gap-4"><h3 className="text-2xl">{service.name}</h3><span className="text-sm font-medium text-forest">{service.price}</span></div><p className="mt-2 text-sm text-muted">{service.duration}</p><p className="mt-4 text-muted leading-relaxed">{service.description}</p><ul className="mt-5 space-y-2 text-sm text-muted">{service.markers.map(marker=><li key={marker}>— {marker}</li>)}</ul></article>}
