import type { ReactNode } from 'react'
export function Badge({children}:{children:ReactNode}){return <span className="inline-flex border border-sage bg-sage-light px-3 py-1 text-xs font-medium text-forest">{children}</span>}
