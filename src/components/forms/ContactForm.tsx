import { useState, type FormEvent } from 'react'
import { Toast } from '../ui/Toast'
export function ContactForm(){const[sent,setSent]=useState(false);function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();if(!event.currentTarget.checkValidity()){event.currentTarget.reportValidity();return}setSent(true);event.currentTarget.reset()}return <form className="space-y-5" onSubmit={submit}><label className="block"><span className="text-sm font-medium">Prénom</span><input required name="firstName" autoComplete="given-name" className="mt-2 w-full border border-sage bg-paper p-3"/></label><label className="block"><span className="text-sm font-medium">Email</span><input type="email" required name="email" autoComplete="email" className="mt-2 w-full border border-sage bg-paper p-3"/></label><label className="block"><span className="text-sm font-medium">Message</span><textarea required name="message" rows={6} className="mt-2 w-full border border-sage bg-paper p-3"/></label><button className="min-h-12 bg-forest px-6 text-paper">Envoyer</button>{sent&&<Toast message="Message préparé. Oria reviendra vers toi par email."/>}</form>}
